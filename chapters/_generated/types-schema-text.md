% Generated by `pqlab figures` (crates/pqlab/src/figures.rs). Do not edit.

```text
message schema {
  required boolean is_paid;
  required int32 quantity (INTEGER(8, signed));
  required int32 store_id (INTEGER(16, unsigned));
  required int64 order_id;
  required float weight_kg;
  required fixed_len_byte_array(4) amount (DECIMAL(9, 2));
  optional binary country (STRING);
  required int32 order_date (DATE);
  required int64 paid_at (TIMESTAMP(MICROS, UTC));
  optional group shipping {
    optional binary city (STRING);
    optional binary postcode (STRING);
  }
}
```

*Computed by the reader from `fixtures/types.parquet` (1824 bytes).*
