//! `pqlab`, the book's Parquet reader on the command line. The commands are a library
//! ([`cli`]) so that the browser can run them too; the binary adds `figures`, which writes the
//! book's generated fragments to disk.

pub mod cli;
