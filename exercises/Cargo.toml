[package]
name = "exercises"
description = "The reader's problems: stubs to fill in, and the tests that say whether they are right."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
parquet-lab = { path = "../crates/parquet-lab" }
